#import "/src/lib.typ": mem

#show: mem.with(page: (width: 10cm, height: auto, margin: 5mm))

= Mem

=== A Typst package for creating efficient, compact memory aids for exams.

Made by and for students who struggle to effectively use memory aids during exams.

#v(1mm)

== Usage

Using `mem` is as simple as the following:
```typ
#import "/src/lib.typ": mem

#show: mem
```
